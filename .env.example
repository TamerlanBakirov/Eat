# Veritabanı — SQLite (geliştirme). Production'da Postgres URL'si girebilirsin.
DATABASE_URL="file:./dev.db"

# Oturum (JWT) imzalama anahtarı — en az 32 karakterlik rastgele bir değer üret.
AUTH_SECRET="change-me-to-a-long-random-string"

# Claude API anahtarı — https://console.anthropic.com adresinden alabilirsin.
ANTHROPIC_API_KEY=sk-ant-...
